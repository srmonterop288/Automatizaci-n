export default class OpenEmailCommand {
    private doc;
    private readonly email;
    constructor(document: Document);
    process(subject: string, timeout?: number): Promise<void>;
    private findEmailBySubject;
    private openEmailInIframe;
}
