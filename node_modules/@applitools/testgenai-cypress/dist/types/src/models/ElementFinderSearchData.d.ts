export default class ElementFinderSearchData {
    mainSelectors: string[];
    possibleSelectors: string[];
    expectedText: string | null;
    constructor(mainSelectors: string[], possibleSelectors?: string[], expectedText?: string | null);
}
